# Shared/Phase 2 dependencies. apps/api_gateway has its own
# requirements.txt for Phase 1's FastAPI service.
supabase
python-dotenv
openai
resend
requests
MetaTrader5  # Windows-only; Forex Division's Market Analytics agent, needs a locally running MT5 terminal
fastapi
uvicorn
tzdata  # Windows doesn't ship the IANA tz database by default -- needed for zoneinfo (agents/forex/_pairs.py)
